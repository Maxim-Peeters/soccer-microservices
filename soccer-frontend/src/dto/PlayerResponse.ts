export interface PlayerResponse {
    playerCode: string;
    firstName: string;
    lastName: string;
    position: string;
    teamCode: string;
    birthDate: string; // Use ISO string format to represent the date
    nationality: string;
}

