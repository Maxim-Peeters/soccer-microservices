export interface SupporterRequest {
    firstName: string;
    lastName: string;
    birthDate: string; // ISO date string format
    email: string;
    teamCode: string;
    playerCode: string;
}